<!-- Modal Delete -->
<div class="modal fade" id="deleteModal{{ $counselor->id }}" tabindex="-1" aria-labelledby="deleteModalLabel{{ $counselor->id }}" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel{{ $counselor->id }}">Konfirmasi Hapus</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Apakah Anda yakin ingin menghapus guru BK <strong>{{ $counselor->name }}</strong>?
            </div>
            <div class="modal-footer">
                <form action="{{ route('counselors.destroy', $counselor) }}" method="POST">
                    @csrf
                    @method('DELETE')
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
                    <button type="submit" class="btn btn-danger">Hapus</button>
                </form>
            </div>
        </div>
    </div>
</div> 